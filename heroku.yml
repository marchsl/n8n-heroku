setup:
    addons:
      - plan: heroku-postgresql
        as: DATABASE

build:
    docker:
        web: Dockerfile
        worker: Dockerfile

run:
  web: bash /entrypoint.sh
  worker:
    command:
      - bash
      - -c
      - |
        export WORKER_MODE=true
        bash /entrypoint.sh