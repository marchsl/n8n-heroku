setup:
    addons:
      - plan: heroku-postgresql
        as: DATABASE

build:
    docker:
        web: Dockerfile
        worker: Dockerfile

run:
  web: bash /entrypoint.sh
  worker: bash /entrypoint.sh worker